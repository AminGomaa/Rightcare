@model IEnumerable<RightCareSite.Models.DataBase.Product_Tble>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2><p align="right" class="col-10">الاصناف</p></h2>

    <p align="right" class="col-md-10">
        @Html.ActionLink("انشاء صنف جديد", "Create")
    </p>
    <table class="table">
        <tr>
            <th>


            </th>

            <th>
                <p>السعر</p>
            </th>
            <th>
                <p>الصنف</p>

            </th>

            <th>
                <p>الكود</p>

            </th>
            <th>
                <p>النوع</p>
            </th>

            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink("تعديل", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("التفاصيل", "Details", new { id = item.Id }) |
                    @Html.ActionLink("حذف", "Delete", new { id = item.Id })
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SelPrice)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Category_Tbl.Category)
                </td>

            </tr>
        }

    </table>
